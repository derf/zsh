## vim:set ft=zsh
psvar[1]=''
[[ -f .todo ]] && psvar[1]+='todo '
[[ -f .fehindex.jpg ]] && psvar[1]+='feh '
[[ -d .hg ]] && psvar[1]+='hg '
[[ -d .git ]] && psvar[1]+='git '
[[ -d .svn ]] && psvar[1]+='svn '
[[ -f Makefile || -f makefile ]] && psvar[1]+='make '
