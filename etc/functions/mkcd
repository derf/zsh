## vim:ft=zsh
## create a directory, then change into it
typeset -i ls=0
typeset dir=${1}

if [[ -z ${dir} ]] {
	echo "Usage: mkcd <directory name>" > /dev/stderr
	return 1
}
if [[ -d ${dir} ]] ls=1
mkdir -p ${dir}
cd ${dir}
if ((ls)) ls --color=auto
